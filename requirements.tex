numpy → numerical operations
pandas → MovieLens data handling
matplotlib → plotting (loss curves, etc.)
seaborn → nicer plots (optional but good)
scikit-learn → metrics (RMSE, preprocessing)
torch → neural networks
torchvision → MNIST dataset loading
jupyter → running notebooks
tqdm → progress bars during training